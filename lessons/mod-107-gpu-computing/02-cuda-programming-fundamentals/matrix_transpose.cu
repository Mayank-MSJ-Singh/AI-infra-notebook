// TODO: Implement matrix transpose
__global__ void matrixTranspose(const float *input, float *output, int rows,
                                int cols) {
  // Your code here
  // output[col * rows + row] = input[row * cols + col]
}

int main() {}